import React, { Component } from 'react';
import Helmet from 'react-helmet';

class Resume extends Component {
  render() {
    return (
      <div>
        <Helmet title="Alex's Resume" />
        Resume page.
      </div>
    );
  }
}

export default Resume;
